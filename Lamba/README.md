Create SNS topic, its sub and S3 bucket  
Then modify policy and add it as a policy to be attached to a IAM role  
Create a Lambda func and attach this policy and upload the code after editiing it  
Trigger this using a eventbridge rule/scheduler
